import { sortIcon } from 'chicago-inspection-analytics/helpers/sort-icon';
import { module, test } from 'qunit';

module('Unit | Helper | sort icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortIcon([42]);
  assert.ok(result);
});
