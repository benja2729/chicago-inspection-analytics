import { floor } from 'chicago-inspection-analytics/helpers/floor';
import { module, test } from 'qunit';

module('Unit | Helper | floor');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = floor([42]);
  assert.ok(result);
});
